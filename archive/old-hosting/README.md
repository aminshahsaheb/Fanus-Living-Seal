# Old Hosting Configs (archived)

These files were used for Railway and Fly.io deployments, both of which
required a credit card once their free trials ended. Fanus now runs on
FastAPI Cloud (https://fanus-living-seal.fastapicloud.dev), configured
via pyproject.toml's [tool.fastapi] section — no Procfile/Dockerfile/
fly.toml/railway.json needed.

Kept here (not deleted) in case a future deploy target needs them again.
